# ASADH (Adaptive Scale-Aware Detection Head) 实验配置
# Baseline + ASADH 检测头增强

# 实验名称
name: asadh_head

# 使用 ASADH 模型配置
model: configs/model/yolo11-obb-asadh.yaml

# 如果需要覆盖其他训练参数，可以在这里添加
# train:
#   epochs: 300
#   batch: 4
#   imgsz: 1024
#   lr0: 0.01

