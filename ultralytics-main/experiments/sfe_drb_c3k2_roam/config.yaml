# C3k2_ROAM 实验配置
# 只写与 configs/train/default.yaml 不同的参数

model: configs/model/yolo11-obb-sfe-drb.yaml

train:
  project: runs/obb/sfe_drb+roam         # 顶层目录
  name: weightsa         # 你的旧实验目录名称（不要写 exp_name）
  resume: D:\code\yolov11_obb_dota\runs\obb\sfe_drb+roam\weights\last.pt               # 自动加载 runs/obb/sfe_drb+roam/ 的 last.pt
# 如需加载本地预训练权重，取消注释并填写路径
# weights: D:\code\yolov11_obb_dota\ultralytics-main\yolo11n.pt

# train:
#   epochs: 300
#   batch: 32

