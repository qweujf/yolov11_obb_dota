# Baseline 实验配置
# 使用原始 YOLO11-OBB 模型（无 MCAttention）

# 实验名称
name: baseline

# 使用原始模型配置（不指定则使用默认 yolov11_obb.yaml）
# model: configs/model/yolov11_obb.yaml

# 如果需要覆盖其他训练参数，可以在这里添加
# train:
#   epochs: 300
#   lr0: 0.01

