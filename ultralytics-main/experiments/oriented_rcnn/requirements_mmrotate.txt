# Oriented R-CNN 环境依赖
# 用于在远程电脑上配置环境

# 基础依赖（如果还没安装）
# torch>=1.8.0
# torchvision>=0.9.0
# torchaudio  # 可选

# mmrotate 框架依赖
# 注意：Windows 上推荐使用 mmcv-lite，Linux 上可以使用 mmcv
# 如果 mmcv-lite 不够用，可以安装预编译的 mmcv-full
mmcv-lite>=2.0.0  # Windows 推荐，或使用 mmcv-full（需要根据 CUDA 版本选择）
mmengine>=0.10.0
mmdet>=2.28.0
mmrotate>=0.3.0

# 其他必需依赖
numpy>=1.23.0
opencv-python>=4.6.0
pyyaml>=5.3.1
tqdm>=4.64.0
matplotlib>=3.3.0
pillow>=7.1.2
scipy>=1.4.1
pandas>=1.1.4

# 可选依赖
# wandb  # 用于实验跟踪
# tensorboard  # 用于可视化




