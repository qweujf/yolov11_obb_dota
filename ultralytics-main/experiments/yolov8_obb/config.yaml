# YOLOv8n-OBB 对比实验配置
# 使用 DOTAv2.0 数据集，与 YOLO_DDBC 实验保持一致

# 实验名称
name: yolov8n_obb_dota

# 模型配置（可选，如果不指定则使用默认 yolov8n-obb.pt）
# model: yolov8n-obb.pt

# 训练配置（可选，会覆盖默认配置）
# train:
#   epochs: 300
#   batch: 4
#   imgsz: 1024
#   lr0: 0.01
#   optimizer: SGD
#   device: 0
#   seed: 42

